bash  
cat \> /home/claude/gerar\_doc.js \<\< 'EOF'  
const {  
  Document, Packer, Paragraph, TextRun, Table, TableRow, TableCell,  
  AlignmentType, HeadingLevel, BorderStyle, WidthType, ShadingType,  
  LevelFormat, PageNumber, PageBreak  
} \= require('docx');  
const fs \= require('fs');

const BRAND\_BLUE \= "1E3A5F";  
const ACCENT \= "2E75B6";  
const LIGHT\_BG \= "EBF3FB";  
const RED\_BG \= "FDECEA";  
const RED\_ACCENT \= "C0392B";  
const GREEN\_BG \= "E8F5E9";  
const GREEN\_ACCENT \= "2E7D32";  
const BORDER\_GRAY \= "D0D0D0";  
const TEXT\_DARK \= "1A1A2E";

const cellBorder \= { style: BorderStyle.SINGLE, size: 1, color: BORDER\_GRAY };  
const noBorder \= { style: BorderStyle.NONE, size: 0, color: "FFFFFF" };  
const allBorders \= { top: cellBorder, bottom: cellBorder, left: cellBorder, right: cellBorder };  
const noBorders \= { top: noBorder, bottom: noBorder, left: noBorder, right: noBorder };

function h1(text) {  
  return new Paragraph({  
    heading: HeadingLevel.HEADING\_1,  
    spacing: { before: 360, after: 120 },  
    border: { bottom: { style: BorderStyle.SINGLE, size: 4, color: ACCENT, space: 4 } },  
    children: \[new TextRun({ text, bold: true, color: BRAND\_BLUE, size: 32, font: "Arial" })\]  
  });  
}

function h2(text) {  
  return new Paragraph({  
    heading: HeadingLevel.HEADING\_2,  
    spacing: { before: 280, after: 80 },  
    children: \[new TextRun({ text, bold: true, color: ACCENT, size: 26, font: "Arial" })\]  
  });  
}

function h3(text) {  
  return new Paragraph({  
    spacing: { before: 200, after: 60 },  
    children: \[new TextRun({ text, bold: true, color: TEXT\_DARK, size: 22, font: "Arial" })\]  
  });  
}

function p(text, opts \= {}) {  
  return new Paragraph({  
    spacing: { before: 60, after: 60 },  
    children: \[new TextRun({ text, size: 20, font: "Arial", color: TEXT\_DARK, ...opts })\]  
  });  
}

function bullet(text) {  
  return new Paragraph({  
    numbering: { reference: "bullets", level: 0 },  
    spacing: { before: 40, after: 40 },  
    children: \[new TextRun({ text, size: 20, font: "Arial", color: TEXT\_DARK })\]  
  });  
}

function code(text) {  
  return new Paragraph({  
    spacing: { before: 60, after: 60 },  
    shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
    children: \[new TextRun({ text, size: 18, font: "Courier New", color: "333333" })\]  
  });  
}

function spacer() {  
  return new Paragraph({ spacing: { before: 60, after: 60 }, children: \[new TextRun("")\] });  
}

function pageBreak() {  
  return new Paragraph({ children: \[new PageBreak()\] });  
}

// Tabela de problemas corrigida  
function problemsTable() {  
  const headers \= \["Problema", "Risco Real", "Correção Aplicada"\];  
  const rows\_data \= \[  
    \["SQLite dev \+ PostgreSQL prod", "Bugs silenciosos só aparecem em prod", "PostgreSQL em todos os ambientes"\],  
    \["Express 5 (beta) \+ ES Modules", "Instabilidade, pacotes incompatíveis", "Express 4 estável \+ CommonJS"\],  
    \["WebSocket sem autenticação", "Qualquer cliente pode se conectar", "JWT validado no handshake WS"\],  
    \["Webhook Evolution sem HMAC", "Injeção de mensagens falsas", "Verificação de assinatura obrigatória"\],  
    \["LLM chamado inline no webhook", "Timeout → Evolution reenvia → duplicatas", "Fila assíncrona com BullMQ"\],  
    \["Sem fila/queue", "Pico derruba servidor ou queima rate limit", "BullMQ \+ Redis para ops pesadas"\],  
    \["Multitenancy não definido", "Schema do banco inconsistente", "Definido como single-tenant"\],  
    \["Módulo financeiro vago", "Escopo aberto que nunca fecha", "Escopo mínimo bem delimitado no MVP"\],  
  \];

  const headerRow \= new TableRow({  
    tableHeader: true,  
    children: headers.map(h \=\> new TableCell({  
      borders: allBorders,  
      shading: { fill: BRAND\_BLUE, type: ShadingType.CLEAR },  
      margins: { top: 80, bottom: 80, left: 120, right: 120 },  
      width: { size: h \=== headers\[0\] ? 2800 : h \=== headers\[1\] ? 3200 : 3360, type: WidthType.DXA },  
      children: \[new Paragraph({ children: \[new TextRun({ text: h, bold: true, color: "FFFFFF", size: 18, font: "Arial" })\] })\]  
    }))  
  });

  const dataRows \= rows\_data.map((\[prob, risk, fix\], i) \=\> new TableRow({  
    children: \[prob, risk, fix\].map((val, j) \=\> new TableCell({  
      borders: allBorders,  
      shading: { fill: i % 2 \=== 0 ? "FFFFFF" : "F5F8FB", type: ShadingType.CLEAR },  
      margins: { top: 80, bottom: 80, left: 120, right: 120 },  
      width: { size: j \=== 0 ? 2800 : j \=== 1 ? 3200 : 3360, type: WidthType.DXA },  
      children: \[new Paragraph({  
        children: \[new TextRun({  
          text: val, size: 18, font: "Arial",  
          color: j \=== 2 ? GREEN\_ACCENT : j \=== 1 ? RED\_ACCENT : TEXT\_DARK  
        })\]  
      })\]  
    }))  
  }));

  return new Table({  
    width: { size: 9360, type: WidthType.DXA },  
    columnWidths: \[2800, 3200, 3360\],  
    rows: \[headerRow, ...dataRows\]  
  });  
}

// Tabela de stack  
function stackTable(title, rows\_data) {  
  const headerRow \= new TableRow({  
    tableHeader: true,  
    children: \["Camada", "Tecnologia", "Motivo"\].map(h \=\> new TableCell({  
      borders: allBorders,  
      shading: { fill: ACCENT, type: ShadingType.CLEAR },  
      margins: { top: 80, bottom: 80, left: 120, right: 120 },  
      width: { size: h \=== "Camada" ? 2200 : h \=== "Tecnologia" ? 3000 : 4160, type: WidthType.DXA },  
      children: \[new Paragraph({ children: \[new TextRun({ text: h, bold: true, color: "FFFFFF", size: 18, font: "Arial" })\] })\]  
    }))  
  });

  const dataRows \= rows\_data.map((\[layer, tech, reason\], i) \=\> new TableRow({  
    children: \[layer, tech, reason\].map((val, j) \=\> new TableCell({  
      borders: allBorders,  
      shading: { fill: i % 2 \=== 0 ? "FFFFFF" : "F5F8FB", type: ShadingType.CLEAR },  
      margins: { top: 80, bottom: 80, left: 120, right: 120 },  
      width: { size: j \=== 0 ? 2200 : j \=== 1 ? 3000 : 4160, type: WidthType.DXA },  
      children: \[new Paragraph({  
        children: \[new TextRun({ text: val, size: 18, font: "Arial", bold: j \=== 1, color: j \=== 1 ? ACCENT : TEXT\_DARK })\]  
      })\]  
    }))  
  }));

  return new Table({  
    width: { size: 9360, type: WidthType.DXA },  
    columnWidths: \[2200, 3000, 4160\],  
    rows: \[headerRow, ...dataRows\]  
  });  
}

// Tabela MVP  
function mvpTable() {  
  const items \= \[  
    \["✅ Autenticação e Usuários", "Login JWT, roles admin/agent, troca de senha"\],  
    \["✅ CRM / Kanban", "Stages configuráveis, drag & drop, atualização tempo real via WebSocket"\],  
    \["✅ WhatsApp Chat", "Webhook \+ HMAC, texto e imagens, interface de chat, assumir conversa"\],  
    \["✅ Agente IA (básico)", "1 agente configurável, fila BullMQ, horário comercial, away message, \[TRANSFERIR\_HUMANO\]"\],  
    \["✅ Contatos", "CRUD completo com histórico de conversas"\],  
    \["✅ Configurações", "Evolution API URL/key, LLM provider/key, horário de funcionamento"\],  
  \];

  const rows \= items.map((\[feat, desc\], i) \=\> new TableRow({  
    children: \[  
      new TableCell({  
        borders: allBorders,  
        shading: { fill: i % 2 \=== 0 ? GREEN\_BG : "FFFFFF", type: ShadingType.CLEAR },  
        margins: { top: 80, bottom: 80, left: 120, right: 120 },  
        width: { size: 2800, type: WidthType.DXA },  
        children: \[new Paragraph({ children: \[new TextRun({ text: feat, size: 18, font: "Arial", bold: true, color: GREEN\_ACCENT })\] })\]  
      }),  
      new TableCell({  
        borders: allBorders,  
        shading: { fill: i % 2 \=== 0 ? "FFFFFF" : "F9F9F9", type: ShadingType.CLEAR },  
        margins: { top: 80, bottom: 80, left: 120, right: 120 },  
        width: { size: 6560, type: WidthType.DXA },  
        children: \[new Paragraph({ children: \[new TextRun({ text: desc, size: 18, font: "Arial", color: TEXT\_DARK })\] })\]  
      })  
    \]  
  }));

  return new Table({  
    width: { size: 9360, type: WidthType.DXA },  
    columnWidths: \[2800, 6560\],  
    rows  
  });  
}

// Tabela Fase 2  
function phase2Table() {  
  const items \= \[  
    "Múltiplos agentes IA com perfis distintos (Maya, Oliver, Lara, Morgana)",  
    "Ordens de serviço com geração de PDF",  
    "Módulo financeiro (entradas/saídas \+ relatório simples)",  
    "Dashboard com gráficos (Recharts)",  
    "Webhooks de entrada para leads (Facebook/Instagram Lead Ads)",  
    "Disparos em massa com fila e rate limiting",  
    "Catálogo de produtos",  
  \];  
  return items.map(item \=\> bullet("🔜 " \+ item));  
}

const doc \= new Document({  
  numbering: {  
    config: \[  
      {  
        reference: "bullets",  
        levels: \[{  
          level: 0, format: LevelFormat.BULLET, text: "\\u2022", alignment: AlignmentType.LEFT,  
          style: { paragraph: { indent: { left: 720, hanging: 360 } } }  
        }\]  
      },  
      {  
        reference: "ordered",  
        levels: \[{  
          level: 0, format: LevelFormat.DECIMAL, text: "%1.", alignment: AlignmentType.LEFT,  
          style: { paragraph: { indent: { left: 720, hanging: 360 } } }  
        }\]  
      }  
    \]  
  },  
  styles: {  
    default: { document: { run: { font: "Arial", size: 20, color: TEXT\_DARK } } },  
    paragraphStyles: \[  
      {  
        id: "Heading1", name: "Heading 1", basedOn: "Normal", next: "Normal", quickFormat: true,  
        run: { size: 32, bold: true, font: "Arial", color: BRAND\_BLUE },  
        paragraph: { spacing: { before: 360, after: 120 }, outlineLevel: 0 }  
      },  
      {  
        id: "Heading2", name: "Heading 2", basedOn: "Normal", next: "Normal", quickFormat: true,  
        run: { size: 26, bold: true, font: "Arial", color: ACCENT },  
        paragraph: { spacing: { before: 280, after: 80 }, outlineLevel: 1 }  
      }  
    \]  
  },  
  sections: \[{  
    properties: {  
      page: {  
        size: { width: 12240, height: 15840 },  
        margin: { top: 1440, right: 1440, bottom: 1440, left: 1440 }  
      }  
    },  
    children: \[  
      // CAPA  
      new Paragraph({ spacing: { before: 1440, after: 0 }, children: \[new TextRun({ text: "MEGA CRM", bold: true, size: 72, font: "Arial", color: BRAND\_BLUE })\] }),  
      new Paragraph({ spacing: { before: 120, after: 80 }, children: \[new TextRun({ text: "Plano Técnico Corrigido", size: 36, font: "Arial", color: ACCENT })\] }),  
      new Paragraph({ spacing: { before: 0, after: 0 }, border: { bottom: { style: BorderStyle.SINGLE, size: 6, color: ACCENT, space: 4 } }, children: \[new TextRun("")\] }),  
      spacer(),  
      p("Versão: 1.0  |  Mega Acessórios Automotivos  |  Empresa única (single-tenant)"),  
      p("Gerado em: 10/06/2026  |  Lucas Borges Studio"),  
      spacer(),  
      new Paragraph({  
        spacing: { before: 120, after: 120 },  
        shading: { fill: LIGHT\_BG, type: ShadingType.CLEAR },  
        children: \[  
          new TextRun({ text: "Este documento substitui a documentação original com correções de arquitetura, escopo redefinido e um plano de desenvolvimento em fases. Zero código existente — partimos do zero com as decisões certas.", size: 20, font: "Arial", color: TEXT\_DARK, italics: true })  
        \]  
      }),

      pageBreak(),

      // SEÇÃO 1  
      h1("1. O que estava errado no plano anterior"),  
      spacer(),  
      problemsTable(),  
      spacer(),

      pageBreak(),

      // SEÇÃO 2  
      h1("2. Decisões de Arquitetura"),  
      spacer(),  
      h2("2.1 Frontend"),  
      stackTable("Frontend", \[  
        \["UI Framework", "React \+ Vite", "Mantido — boa escolha, alta performance"\],  
        \["Estilização", "TailwindCSS", "Substitui CSS manual, mais consistente e rápido"\],  
        \["Estado global", "Zustand", "Leve, sem boilerplate do Redux"\],  
        \["WebSocket", "Socket.io-client", "Par do backend, com fallback automático"\],  
        \["Cache/Sincronização", "React Query (TanStack)", "Cache inteligente, evita refetch desnecessário"\],  
      \]),  
      spacer(),  
      h2("2.2 Backend"),  
      stackTable("Backend", \[  
        \["Runtime", "Node.js \+ Express 4", "Express 4 estável — não usar 5 (ainda beta)"\],  
        \["Módulos", "CommonJS", "Compatibilidade total com ecossistema npm"\],  
        \["WebSocket", "Socket.io", "Fallback automático, rooms por conversa"\],  
        \["Fila de jobs", "BullMQ \+ Redis", "Worker separado para LLM e disparos pesados"\],  
        \["Upload", "Multer", "Mantido — funciona bem para mídia"\],  
        \["Autenticação", "JWT \+ bcrypt", "Padrão sólido, sem overhead"\],  
        \["Validação", "Zod", "Schemas tipados, erros claros"\],  
      \]),  
      spacer(),  
      h2("2.3 Banco de Dados e Infraestrutura"),  
      stackTable("Infra", \[  
        \["Banco (todos envs)", "PostgreSQL", "Mesmo banco dev e prod — elimina bugs silenciosos"\],  
        \["Query builder", "Knex.js", "Migrations versionadas, sem ORM pesado"\],  
        \["Cache \+ Fila", "Redis", "BullMQ \+ cache de sessão"\],  
        \["Deploy", "Docker \+ Coolify", "Mantido — funciona bem"\],  
        \["Proxy reverso", "Nginx", "Mantido"\],  
        \["Domínio", "crm.megaacessoriosautomotivos.com.br", "Mantido"\],  
      \]),  
      spacer(),

      pageBreak(),

      // SEÇÃO 3  
      h1("3. Fluxo Crítico — Webhook \+ IA (Corrigido)"),  
      spacer(),  
      p("O ponto de maior risco do sistema original era chamar o LLM de forma síncrona dentro do handler do webhook. Isso causaria timeout no Evolution API → reenvio do evento → respostas duplicadas para o cliente final.", { italics: true }),  
      spacer(),  
      h2("Fluxo correto (assíncrono):"),  
      spacer(),  
      new Paragraph({  
        shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
        spacing: { before: 80, after: 80 },  
        children: \[new TextRun({ text: "Evolution API → POST /api/webhook/evolution", size: 18, font: "Courier New" })\]  
      }),  
      new Paragraph({  
        shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 0 },  
        children: \[new TextRun({ text: "  1\. Valida assinatura HMAC (rejeita se inválida)", size: 18, font: "Courier New" })\]  
      }),  
      new Paragraph({  
        shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 0 },  
        children: \[new TextRun({ text: '  2\. Responde HTTP 200 imediatamente (\< 500ms) ← chave', size: 18, font: "Courier New", bold: true })\]  
      }),  
      new Paragraph({  
        shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 0 },  
        children: \[new TextRun({ text: "  3\. Salva mensagem bruta no banco", size: 18, font: "Courier New" })\]  
      }),  
      new Paragraph({  
        shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 0 },  
        children: \[new TextRun({ text: "  4\. Enfileira job no BullMQ → fila 'message-processor'", size: 18, font: "Courier New" })\]  
      }),  
      new Paragraph({  
        shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 80 },  
        children: \[new TextRun({ text: "       ↓ Worker (processo separado, sem limite de tempo)", size: 18, font: "Courier New" })\]  
      }),  
      new Paragraph({  
        shading: { fill: "F0F0F0", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 80 },  
        children: \[new TextRun({ text: "       Carrega contexto → verifica horário/agente humano\\n       → chama LLM → salva → envia via Evolution API\\n       → broadcast WebSocket para frontend", size: 18, font: "Courier New" })\]  
      }),  
      spacer(),  
      p("Resultado: Evolution API recebe 200 OK em \< 500ms. Nunca reenvia. O LLM tem todo o tempo que precisa no worker.", { bold: true, color: GREEN\_ACCENT }),  
      spacer(),

      pageBreak(),

      // SEÇÃO 4  
      h1("4. Schema do Banco de Dados"),  
      spacer(),  
      h2("Tabelas principais"),  
      spacer(),  
      ...\[  
        \["users", "id, name, email, password\_hash, role (admin | agent | viewer), created\_at"\],  
        \["contacts", "id, name, phone, email, source, status, assigned\_to (FK users), created\_at"\],  
        \["pipeline\_stages", "id, name, order\_index, color"\],  
        \["pipeline\_cards", "id, contact\_id (FK), stage\_id (FK), assigned\_to (FK), notes, timestamps"\],  
        \["conversations", "id, contact\_id (FK), channel, status (open|bot|human|closed), assigned\_agent\_id"\],  
        \["messages", "id, conversation\_id (FK), direction, content\_type, content, media\_url, sent\_by, created\_at"\],  
        \["ai\_agents", "id, name, role, system\_prompt, llm\_model, temperature, typing\_delay\_ms, is\_active"\],  
        \["automation\_config", "id (single row), api keys criptografadas, business\_hours, away\_message"\],  
        \["service\_orders", "id, contact\_id (FK), title, description, status, total\_value (Fase 2)"\],  
        \["financial\_entries", "id, type, category, description, amount, date (Fase 2)"\],  
        \["products", "id, name, description, price, unit, is\_active (Fase 2)"\],  
      \].map((\[table, desc\]) \=\> new Table({  
        width: { size: 9360, type: WidthType.DXA },  
        columnWidths: \[2200, 7160\],  
        rows: \[new TableRow({  
          children: \[  
            new TableCell({  
              borders: allBorders,  
              shading: { fill: LIGHT\_BG, type: ShadingType.CLEAR },  
              margins: { top: 60, bottom: 60, left: 120, right: 120 },  
              width: { size: 2200, type: WidthType.DXA },  
              children: \[new Paragraph({ children: \[new TextRun({ text: table, bold: true, size: 18, font: "Courier New", color: ACCENT })\] })\]  
            }),  
            new TableCell({  
              borders: allBorders,  
              shading: { fill: "FFFFFF", type: ShadingType.CLEAR },  
              margins: { top: 60, bottom: 60, left: 120, right: 120 },  
              width: { size: 7160, type: WidthType.DXA },  
              children: \[new Paragraph({ children: \[new TextRun({ text: desc, size: 18, font: "Courier New", color: TEXT\_DARK })\] })\]  
            })  
          \]  
        })\]  
      })),  
      spacer(),

      pageBreak(),

      // SEÇÃO 5  
      h1("5. Escopo do MVP — Fase 1"),  
      spacer(),  
      p("Estimativa: 8–12 semanas. Entrega de um sistema funcional e estável antes de adicionar complexidade."),  
      spacer(),  
      mvpTable(),  
      spacer(),  
      h2("Fase 2 — Após validação do MVP"),  
      spacer(),  
      ...phase2Table(),  
      spacer(),

      pageBreak(),

      // SEÇÃO 6  
      h1("6. Estrutura do Projeto"),  
      spacer(),  
      ...\[  
        "mega-crm/",  
        "  ├── docker-compose.yml",  
        "  ├── docker-compose.dev.yml",  
        "  ├── .env.example",  
        "  │",  
        "  ├── backend/",  
        "  │   ├── src/",  
        "  │   │   ├── config/          \# db, redis, env",  
        "  │   │   ├── middleware/      \# auth, errorHandler, webhookValidator",  
        "  │   │   ├── routes/          \# agents, contacts, conversations, webhook...",  
        "  │   │   ├── workers/         \# messageProcessor.worker.js",  
        "  │   │   ├── services/        \# evolutionApi.service.js, llm.service.js",  
        "  │   │   ├── db/migrations/",  
        "  │   │   └── app.js",  
        "  │   └── Dockerfile",  
        "  │",  
        "  └── frontend/",  
        "      ├── src/",  
        "      │   ├── components/      \# UI atômico",  
        "      │   ├── pages/           \# Dashboard, Kanban, Chat, Contacts, Settings",  
        "      │   ├── stores/          \# Zustand stores",  
        "      │   ├── hooks/           \# useSocket, useConversation...",  
        "      │   ├── services/        \# api.js (Axios)",  
        "      │   └── main.jsx",  
        "      └── Dockerfile",  
      \].map(line \=\> new Paragraph({  
        shading: { fill: "F5F5F5", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 0 },  
        children: \[new TextRun({ text: line, size: 18, font: "Courier New", color: "333333" })\]  
      })),  
      spacer(),

      pageBreak(),

      // SEÇÃO 7  
      h1("7. Variáveis de Ambiente"),  
      spacer(),  
      ...\[  
        "\# Backend",  
        "NODE\_ENV=production",  
        "PORT=3001",  
        "DATABASE\_URL=postgresql://user:pass@postgres:5432/megacrm",  
        "REDIS\_URL=redis://redis:6379",  
        "JWT\_SECRET=\<string longa e aleatória\>",  
        "",  
        "\# Evolution API",  
        "EVOLUTION\_API\_URL=https://...",  
        "EVOLUTION\_API\_KEY=...",  
        "EVOLUTION\_WEBHOOK\_SECRET=\<secret para validar HMAC\>",  
        "",  
        "\# LLM",  
        "LLM\_PROVIDER=groq",  
        "LLM\_API\_KEY=...",  
        "LLM\_MODEL=llama-3.3-70b-versatile",  
        "",  
        "\# Frontend (build time)",  
        "VITE\_API\_URL=https://crm.megaacessoriosautomotivos.com.br/api",  
        "VITE\_WS\_URL=wss://crm.megaacessoriosautomotivos.com.br",  
      \].map(line \=\> new Paragraph({  
        shading: { fill: "F0F4F8", type: ShadingType.CLEAR },  
        spacing: { before: 0, after: 0 },  
        children: \[new TextRun({ text: line || " ", size: 18, font: "Courier New", color: line.startsWith("\#") ? "6B8E23" : "1A1A2E" })\]  
      })),  
      spacer(),

      // SEÇÃO 8  
      h1("8. Ordem de Desenvolvimento"),  
      spacer(),  
      ...\[  
        "1. Setup base: Docker Compose \+ PostgreSQL \+ Redis \+ migrations rodando",  
        "2. Backend: autenticação JWT (login, refresh, roles)",  
        "3. Backend: rotas de contatos e pipeline (CRUD completo)",  
        "4. Frontend: tela de login \+ Kanban com dados reais",  
        "5. Backend: webhook Evolution \+ validação HMAC \+ enfileiramento BullMQ",  
        "6. Backend: worker de processamento \+ integração LLM",  
        "7. Frontend: interface de chat em tempo real (Socket.io)",  
        "8. Testes ponta a ponta: WhatsApp → IA → Chat → assumir atendimento",  
        "9. MVP validado com o cliente → iniciar Fase 2",  
      \].map(item \=\> new Paragraph({  
        numbering: { reference: "ordered", level: 0 },  
        spacing: { before: 60, after: 60 },  
        children: \[new TextRun({ text: item.replace(/^\\d+\\. /, ''), size: 20, font: "Arial", color: TEXT\_DARK })\]  
      })),  
      spacer(),  
      spacer(),  
      new Paragraph({  
        spacing: { before: 200, after: 0 },  
        border: { top: { style: BorderStyle.SINGLE, size: 2, color: BORDER\_GRAY, space: 4 } },  
        children: \[new TextRun({ text: "Mega CRM — Plano Técnico Corrigido v1.0 | Lucas Borges Studio | 10/06/2026", size: 16, font: "Arial", color: "888888", italics: true })\]  
      }),  
    \]  
  }\]  
});

Packer.toBuffer(doc).then(buffer \=\> {  
  fs.writeFileSync('/mnt/user-data/outputs/Mega\_CRM\_Plano\_Tecnico.docx', buffer);  
  console.log('Arquivo gerado com sucesso\!');  
}).catch(err \=\> console.error(err));  
EOF  
node /home/claude/gerar\_doc.js  
Saída  
Arquivo gerado com sucesso\!  
